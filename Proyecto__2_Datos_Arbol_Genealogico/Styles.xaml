<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Colores base -->
    <Color x:Key="BaseDark">#0D0D0D</Color>
    <Color x:Key="PanelDark">#161616</Color>
    <Color x:Key="BorderDark">#222</Color>
    <Color x:Key="AccentBlue">#1754E8</Color>
    <Color x:Key="TextLight">#EAEAEA</Color>
    <Color x:Key="TextGray">#9A9A9A</Color>

    <!-- Brush -->
    <SolidColorBrush x:Key="BaseDarkBrush" Color="{StaticResource BaseDark}"/>
    <SolidColorBrush x:Key="PanelDarkBrush" Color="{StaticResource PanelDark}"/>
    <SolidColorBrush x:Key="BorderDarkBrush" Color="{StaticResource BorderDark}"/>
    <SolidColorBrush x:Key="AccentBlueBrush" Color="{StaticResource AccentBlue}"/>
    <SolidColorBrush x:Key="TextLightBrush" Color="{StaticResource TextLight}"/>
    <SolidColorBrush x:Key="TextGrayBrush" Color="{StaticResource TextGray}"/>

    <!-- Estilo de ventana -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource BaseDarkBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <!-- Estilo de borde general -->
    <Style TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PanelDarkBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderDarkBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
    </Style>
    

    <!-- TextBlocks -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
    </Style>

    <!-- TextBoxes -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="#111"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
    </Style>

    <!-- DatePicker -->
    <Style TargetType="DatePicker">
        <Setter Property="Background" Value="#000"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
    </Style>

    <!-- ListBox -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="#111"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
    </Style>

    <!-- Botones -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentBlueBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="0,4,0,0"/>
    </Style>

</ResourceDictionary>
